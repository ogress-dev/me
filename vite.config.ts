// Configure Vite for a static SPA build.
// This repository was originally set up for TanStack Start SSR. To deploy as a
// static single-page app (no server), keep the default Vite config provided by
// the tanstack config package but do not enable the server entry.
import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig({});
